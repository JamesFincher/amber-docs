# Convex (created by `pnpm convex dev`)
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
NEXT_PUBLIC_CONVEX_SITE_URL=https://your-deployment.convex.site

# Used for sitemap generation (optional but recommended in production)
SITE_URL=https://docs.yourcompany.com

# Google AI (Gemini)
# This app calls Gemini directly from the browser, so the API key is entered in the UI and stored in localStorage.
# You can still set a default model for all users at build time:
NEXT_PUBLIC_GEMINI_DEFAULT_MODEL=gemini-3-flash

# Admin gate for the editor UI (`/admin`) and its API (`/api/admin/*`)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

# Shared secret for write access (must be set both in Convex env and in your web host env)
DOCS_WRITE_SECRET=change-me-too
