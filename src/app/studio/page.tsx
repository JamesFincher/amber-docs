import { StudioClient } from "@/components/StudioClient";

export const metadata = {
  title: "Write + publish | Amber Docs",
  description: "Create, edit, publish, and unpublish document files in plain language.",
};

export default function StudioPage() {
  return <StudioClient />;
}
